# Vercel installs from this root file for the Python function (api/index.py).
# Backend is stdlib + these; the LLM is the hosted Groq API (httpx), no GPU deps.
fastapi>=0.115
pydantic>=2.7
httpx>=0.27
